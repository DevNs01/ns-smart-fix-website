{
  "name": "ns-smart-fix-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "node scripts/lint.mjs",
    "typecheck": "node --check src/main.js && node --check support.js && node --check scripts/verify.mjs && node --check scripts/security-test.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:security": "node scripts/security-test.mjs",
    "check": "npm run lint && npm run typecheck && npm test && npm run build && node scripts/verify.mjs && npm run test:security"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  }
}
